package com.bubblewrap.bubble;

import java.math.BigInteger;

public class DirectDecrypter {
    public void decrypt(BigInteger key, String fileName) {
        //read from file and get the encrypted password, then divide the encrypted password using the key to get the password before encryption
    }
}
